---
### Live profile
spring:
  profiles: live
  datasource:
    driver-class-name: ${spring.datasource.driver-class-name}
    username: ${spring.datasource.username.billpay-persistence-service}
    password: ${spring.datasource.password.billpay-persistence-service}
    url: ${spring.datasource.url.billpay-persistence-service}

logging:
  config: ${backbase.config.dir}/billpay-persistence-service/logback.xml

