---
### Live profile
spring:
  profiles: live
  datasource:
    driver-class-name: ${spring.datasource.driver-class-name}
    username: ${spring.datasource.username.actions-pandp-service}
    password: ${spring.datasource.password.actions-pandp-service}
    url: ${spring.datasource.url.actions-pandp-service}

logging:
  config: ${backbase.config.dir}/actions-pandp-service/logback.xml


